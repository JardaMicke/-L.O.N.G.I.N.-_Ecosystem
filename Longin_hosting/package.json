{
  "name": "longin-hosting-monorepo",
  "version": "1.0.0",
  "description": "Longin Hosting - Self-hosted Application Runtime",
  "private": true,
  "workspaces": [
    "services/*"
  ],
  "scripts": {
    "dev": "pnpm -r dev",
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "clean": "rm -rf node_modules services/*/node_modules"
  },
  "devDependencies": {
    "concurrently": "^8.2.0",
    "typescript": "^5.1.6"
  },
  "packageManager": "pnpm@8.6.0",
  "engines": {
    "node": ">=20.0.0"
  }
}
