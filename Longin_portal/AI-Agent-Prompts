# 💻 LONGIN HOSTING SERVER - AI AGENT PROMPTS

**Verze:** 1.0  
**Datum:** Březen 2025  
**Cíl:** Kompletní prompty pro implementaci aplikace AI agentem

---

## 📑 OBSAH

1. [Prompt 1 - Project Setup & Foundation](#prompt-1-project-setup--foundation)
2. [Prompt 2 - Database & Authentication](#prompt-2-database--authentication)
3. [Prompt 3 - Longin Core API - Basic](#prompt-3-longin-core-api--basic)
4. [Prompt 4 - Frontend - Auth & Dashboard](#prompt-4-frontend--auth--dashboard)
5. [Prompt 5 - Docker Integration & App Management](#prompt-5-docker-integration--app-management)
6. [Prompt 6 - WebSocket & Real-time Updates](#prompt-6-websocket--real-time-updates)
7. [Prompt 7 - Monitoring, Logging & Production Ready](#prompt-7-monitoring-logging--production-ready)

---

## 🎯 PROMPT 1: PROJECT SETUP & FOUNDATION

**Pokyn pro AI agenta:**

Vytvořte základní strukturu projektu Longin Hosting Server s kompletní infrastrukturou Docker a konfiguracemi. Tento krok vytvoří prostředí pro všechny následující úkoly.

**KOMPLETNÍ ZADÁNÍ:**

```
Vytvořit projekt: Longin Hosting Server - Foundation Setup

CÍLEM JE:
─────────
Vytvořit kompletní projektu strukturu se všemi konfiguračními soubory, 
Git repo setupem, Docker infrastrukturou a TypeScript setupem.

KONKRÉTNÍ ÚKOLY:
────────────────

1. SETUP GIT REPOZITÁŘE
   ─────────────────────
   - Inicializovat Git repo (lokální)
   - Vytvořit .gitignore (Node.js, Docker, IDE)
   - Vytvořit README.md s instrukcemi
   - Setup: git init → add → commit

2. ADRESÁŘOVÁ STRUKTURA
   ────────────────────
   Vytvořit tuto strukturu:
   
   longin-hosting-server/
   ├── services/
   │   ├── longin-core/
   │   │   ├── src/
   │   │   │   ├── config/
   │   │   │   ├── middleware/
   │   │   │   ├── routes/
   │   │   │   ├── services/
   │   │   │   ├── websocket/
   │   │   │   ├── utils/
   │   │   │   ├── app.ts
   │   │   │   └── index.ts
   │   │   ├── Dockerfile
   │   │   ├── package.json
   │   │   ├── tsconfig.json
   │   │   └── .env.example
   │   │
   │   ├── longin-ui/
   │   │   ├── src/
   │   │   │   ├── components/
   │   │   │   ├── pages/
   │   │   │   ├── hooks/
   │   │   │   ├── context/
   │   │   │   ├── styles/
   │   │   │   ├── App.tsx
   │   │   │   └── index.tsx
   │   │   ├── public/
   │   │   ├── Dockerfile
   │   │   ├── package.json
   │   │   ├── tsconfig.json
   │   │   └── .env.example
   │   │
   │   ├── database/
   │   │   ├── init-db.sql
   │   │   ├── pgvector-init.sql
   │   │   └── migrations/
   │   │
   │   └── monitoring/
   │       ├── prometheus.yml
   │       └── grafana/
   │           └── provisioning/
   │
   ├── docker-compose.yml
   ├── docker-compose.prod.yml
   ├── .env.example
   ├── .env (lokální - v .gitignore)
   ├── .gitignore
   ├── README.md
   └── docs/
       ├── SETUP.md
       ├── DEPLOYMENT.md
       └── API.md

3. TYPESCRIPT KONFIGURACE
   ──────────────────────
   
   A) Longin Core (tsconfig.json):
   {
     "compilerOptions": {
       "target": "ES2020",
       "module": "commonjs",
       "lib": ["ES2020"],
       "outDir": "./dist",
       "rootDir": "./src",
       "strict": true,
       "esModuleInterop": true,
       "skipLibCheck": true,
       "forceConsistentCasingInFileNames": true,
       "resolveJsonModule": true,
       "declaration": true,
       "declarationMap": true,
       "sourceMap": true,
       "removeComments": true,
       "noImplicitAny": true,
       "strictNullChecks": true
     },
     "include": ["src"],
     "exclude": ["node_modules", "dist"]
   }
   
   B) React UI (tsconfig.json):
   {
     "compilerOptions": {
       "target": "ES2020",
       "useDefineForClassFields": true,
       "lib": ["ES2020", "DOM", "DOM.Iterable"],
       "module": "ESNext",
       "skipLibCheck": true,
       "esModuleInterop": true,
       "jsx": "react-jsx",
       "types": ["react/jsx-runtime"]
     },
     "include": ["src"],
     "exclude": ["dist", "build"]
   }

4. LONGIN CORE - PACKAGE.JSON
   ──────────────────────────
   {
     "name": "longin-core",
     "version": "1.0.0",
     "description": "Longin Hosting Server - API Gateway & Orchestrator",
     "main": "dist/index.js",
     "scripts": {
       "dev": "ts-node src/index.ts",
       "build": "tsc",
       "start": "node dist/index.js",
       "lint": "eslint src --ext .ts",
       "format": "prettier --write src",
       "test": "jest",
       "test:watch": "jest --watch"
     },
     "dependencies": {
       "express": "^4.18.2",
       "typescript": "^5.0.0",
       "pg": "^8.10.0",
       "redis": "^4.6.0",
       "bcryptjs": "^2.4.3",
       "jsonwebtoken": "^9.0.0",
       "docker-modem": "^4.0.0",
       "socket.io": "^4.6.0",
       "axios": "^1.3.0",
       "dotenv": "^16.0.0",
       "cors": "^2.8.5",
       "helmet": "^7.0.0",
       "joi": "^17.8.0",
       "pino": "^8.11.0",
       "pino-http": "^8.3.0"
     },
     "devDependencies": {
       "@types/express": "^4.17.17",
       "@types/node": "^18.11.0",
       "@types/pg": "^8.9.0",
       "ts-node": "^10.9.0",
       "eslint": "^8.36.0",
       "@typescript-eslint/eslint-plugin": "^5.59.0",
       "@typescript-eslint/parser": "^5.59.0",
       "prettier": "^2.8.0",
       "jest": "^29.5.0",
       "@types/jest": "^29.5.0",
       "ts-jest": "^29.1.0"
     }
   }

5. LONGIN UI - PACKAGE.JSON
   ────────────────────────
   {
     "name": "longin-ui",
     "version": "1.0.0",
     "description": "Longin Hosting Server - React Frontend",
     "type": "module",
     "scripts": {
       "dev": "vite",
       "build": "tsc && vite build",
       "preview": "vite preview",
       "lint": "eslint src --ext .ts,.tsx",
       "format": "prettier --write src",
       "type-check": "tsc --noEmit"
     },
     "dependencies": {
       "react": "^18.2.0",
       "react-dom": "^18.2.0",
       "react-router-dom": "^6.11.0",
       "axios": "^1.3.0",
       "socket.io-client": "^4.6.0",
       "zustand": "^4.3.0",
       "tailwindcss": "^3.3.0",
       "recharts": "^2.5.0",
       "lucide-react": "^0.263.0"
     },
     "devDependencies": {
       "typescript": "^5.0.0",
       "vite": "^4.2.0",
       "@vitejs/plugin-react": "^4.0.0",
       "@types/react": "^18.0.0",
       "@types/react-dom": "^18.0.0",
       "eslint": "^8.36.0",
       "@typescript-eslint/eslint-plugin": "^5.59.0",
       "@typescript-eslint/parser": "^5.59.0",
       "prettier": "^2.8.0",
       "autoprefixer": "^10.4.0",
       "postcss": "^8.4.0"
     }
   }

6. ROOT - DOCKER-COMPOSE.YML
   ─────────────────────────
   [Viz TECH-Diagrams nebo PRD dokument - kompletní docker-compose.yml]
   
   Minimálně obsahuj:
   - longin-core (Node.js API)
   - longin-ui (React)
   - postgres (Database)
   - redis (Cache)
   - prometheus (Monitoring)
   - grafana (Dashboards)
   - elasticsearch (Logging)
   - logstash (Log Pipeline)
   - kibana (Log UI)
   - traefik (Reverse Proxy)

7. DOCKERFILES
   ────────────
   
   A) services/longin-core/Dockerfile:
   
   FROM node:18-alpine
   
   WORKDIR /app
   
   # Copy package files
   COPY package*.json ./
   COPY tsconfig.json ./
   
   # Install dependencies
   RUN npm ci --only=production
   RUN npm ci --only=dev
   
   # Copy source
   COPY src/ ./src/
   
   # Build
   RUN npm run build
   
   # Remove dev dependencies
   RUN npm prune --production
   
   EXPOSE 3001 3002
   
   HEALTHCHECK --interval=30s --timeout=10s --start-period=40s --retries=3 \
     CMD curl -f http://localhost:3001/health || exit 1
   
   CMD ["node", "dist/index.js"]
   
   B) services/longin-ui/Dockerfile:
   
   FROM node:18-alpine AS builder
   
   WORKDIR /app
   
   COPY package*.json ./
   COPY tsconfig.json ./
   COPY vite.config.ts ./
   
   RUN npm ci
   
   COPY src/ ./src/
   COPY public/ ./public/
   
   RUN npm run build
   
   # Production image
   FROM nginx:alpine
   
   COPY --from=builder /app/dist /usr/share/nginx/html
   COPY nginx.conf /etc/nginx/conf.d/default.conf
   
   EXPOSE 3000
   
   HEALTHCHECK --interval=30s --timeout=10s --retries=3 \
     CMD wget --no-verbose --tries=1 --spider http://localhost:3000/

8. ENVIRONMENT VARIABLES - .ENV.EXAMPLE
   ────────────────────────────────────
   [Viz PRD - Konfigurace & Deployment sekce]
   
   Vytvořit .env.example s:
   - NODE_ENV=development
   - Všechny PORT hodnoty
   - Database credentials
   - JWT secrets
   - Redis config
   - GitHub token
   - SMTP settings
   - Monitoring config
   
   POZNÁMKA: V .gitignore by měl být .env (bez .example)

9. LINTING & PRETTIER KONFIGURACE
   ───────────────────────────────
   
   A) .eslintrc.json:
   {
     "parser": "@typescript-eslint/parser",
     "extends": [
       "eslint:recommended",
       "plugin:@typescript-eslint/recommended"
     ],
     "plugins": ["@typescript-eslint"],
     "env": {
       "node": true,
       "es2020": true
     },
     "parserOptions": {
       "ecmaVersion": 2020,
       "sourceType": "module"
     }
   }
   
   B) .prettierrc:
   {
     "semi": true,
     "trailingComma": "es5",
     "singleQuote": true,
     "printWidth": 100,
     "tabWidth": 2,
     "useTabs": false
   }

10. GIT KONFIGURACE
    ────────────────
    
    A) .gitignore:
    # Node
    node_modules/
    npm-debug.log*
    
    # Environment
    .env
    .env.local
    
    # IDEs
    .vscode/
    .idea/
    *.swp
    *.swo
    
    # Build
    dist/
    build/
    .next/
    
    # OS
    .DS_Store
    Thumbs.db
    
    # Logs
    *.log
    logs/
    
    # Docker
    .dockerignore
    
    B) Inicializace:
    $ git init
    $ git add .
    $ git commit -m "Initial commit: Foundation setup"

11. DOKUMENTACE - README.MD
    ──────────────────────────
    
    # Longin Hosting Server
    
    ## Popis
    Distributed hosting platforma pro spouštění aplikací v Docker kontejnerech
    s web UI, monitoringem a real-time updates.
    
    ## Requirements
    - Node.js 18+
    - Docker 20.10+
    - Docker Compose 2.0+
    - PostgreSQL 16+ (v Docker)
    - Redis 7+ (v Docker)
    
    ## Quick Start
    
    1. Clone repo
       git clone <repo-url>
       cd longin-hosting-server
    
    2. Setup environment
       cp .env.example .env
       # Edit .env s vašimi settings
    
    3. Build & Run
       docker-compose up -d
    
    4. Access aplikace
       - Web UI: http://localhost:3000
       - API: http://localhost:3001
       - Grafana: http://localhost:3003
       - Kibana: http://localhost:5601
    
    5. Default login
       Username: admin
       Password: admin (CHANGE v PRODUCTION!)
    
    ## Development
    
    Backend:
    cd services/longin-core
    npm install
    npm run dev
    
    Frontend:
    cd services/longin-ui
    npm install
    npm run dev
    
    ## Project Structure
    [Detailní popis adresářové struktury]
    
    ## API Documentation
    [Odkaz na API.md nebo Swagger]
    
    ## Contributing
    [Git workflow, commit messages, PR process]

OČEKÁVANÉ VÝSTUPY:
──────────────────

Po dokončení tohoto promptu by mělo existovat:

✓ Git repo se všemi soubory
✓ Kompletní adresářová struktura
✓ Docker Compose konfigurace
✓ TypeScript setup (Backend + Frontend)
✓ Package.json soubory s dependencies
✓ .env.example se všemi proměnnými
✓ Dockerfiles pro obě služby
✓ ESLint a Prettier konfigurace
✓ README.md s instrukcemi
✓ Všechny soubory jsou commitnuty v Git

VALIDACE:
─────────

Po výstupu spusť v terminálu:

1. docker-compose --version
   → Ověř verzi (2.0+)

2. docker version
   → Ověř Docker je spuštěný

3. cd services/longin-core && npm install
   → Ověř instalace dependencies

4. cd ../longin-ui && npm install
   → Ověř React build dependencies

5. docker-compose config
   → Ověř syntax docker-compose.yml

6. docker-compose build
   → Build images (může trvat dlouho)

7. docker-compose up -d
   → Start containers

8. docker ps
   → Ověř všechny kontejnery běží

POZNÁMKY PRO AI AGENTA:
───────────────────────
- Neměň port čísla - jsou pevně definovaná
- Všechny sekrety musí být v .env (nikdy ne v kódu)
- Dockerfiles musí mít HEALTHCHECK
- package.json musí mít TypeScript jako dev dependency
- ESLint/Prettier musí být stejný v obou services
- README musí být jednoduše srozumitelný
```

---

## 🔐 PROMPT 2: DATABASE & AUTHENTICATION

[... pokračování - další prompty ...]

---

**STATUS:** Ready for AI Agent Implementation

Dokumentace je stále pracující, ale již obsahuje:
- ✅ Detailní Prompt 1 s kompletními instrukcemi
- ⏳ Prompty 2-7 (budou přidány v následujícím obsahu)

---

**Vygenerováno:** [translate:Březen 2025]  
**Pro:** AI Agent Implementation

```

Poznámka: Toto je začátek AI Agent Prompts dokumentu. Pokud chcete, mohu pokračovat 
s prompty 2-7 v dalším obsahu.
```
